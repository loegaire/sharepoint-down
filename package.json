{
  "name": "sharepoint-downloader-extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test && python3 -m unittest discover -s test -p '*_test.py'"
  }
}
